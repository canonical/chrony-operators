# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

[project]
name = "chrony-operator"
version = "0.0.0"
description = "Chrony NTP server charm."
requires-python = ">=3.10"
dependencies = [
  "chrony-shared",
  "ops==3.8.1",
]

[tool.uv]
package = false

[tool.uv.sources]
chrony-shared = { path = "../shared" }

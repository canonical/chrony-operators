# Copyright 2026 Canonical Ltd.
# See LICENSE file for licensing details.

[project]
name = "chrony-client-operator"
version = "0.0.0"
description = "Chrony NTP client charm."
requires-python = ">=3.10"
dependencies = [
  "chrony-shared",
  "ops==3.8.2",
]

[tool.uv]
package = false

[tool.uv.sources]
chrony-shared = { workspace = true }
